## Linked issue

<!-- Required. Use a closing keyword so GitHub auto-links and auto-closes
     the issue on merge. Use "Part of #123" instead if this PR only
     partially completes it (no auto-close). -->

Closes #

## What changed

<!-- Brief summary. If the "why" isn't obvious from the linked issue, say
     it here too. -->

## Screenshots / recording

<!-- Required for any visual/UI change. Delete this section otherwise. -->

## Checklist

- [ ] `npm run build` passes locally
- [ ] `npx tsc --noEmit` passes locally
- [ ] `npm run lint` passes locally
- [ ] No new broken internal links introduced
- [ ] Checked in a browser (not just the build) for UI/content changes
