# Brand Shell + Design System

**Priority:** P0
**Phase:** 1
**Status:** Done

Next.js project scaffold, design tokens, typography, and the full library
of reusable page blocks (Hero, FAQ, Pricing, CTA, etc.) that every hub and
service page is built from.

## Scope

- Next.js + TypeScript + Tailwind v4 scaffold
- Design tokens (colors, fonts, spacing) in `src/app/globals.css`
- Header (mega-menu desktop, drawer mobile) + Footer
- Reusable blocks in `src/components/blocks/`
- `/api/contact` route (validation + honeypot)

## Outcome

Done. See `PROGRESS.md` Phase 1 for the verification record (build/lint/
typecheck all clean).
